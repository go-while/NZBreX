builds:
  - id: linux
    main: .
    binary: NZBreX
    goos:
      - linux
      - darwin
      - freebsd
      - openbsd
      - netbsd
    goarch:
      - amd64
      - arm64
      - 386
      - arm
    ldflags:
      - "-s -w -X main.appVersion={{.Version}}"
    env:
      - CGO_ENABLED=0

  - id: windows
    main: .
    binary: NZBreX
    goos:
      - windows
    goarch:
      - amd64
      - arm64
      - 386
      - arm
    ldflags:
      - "-s -w -X main.appVersion={{.Version}}"
    env:
      - CGO_ENABLED=0

archives:
  - id: linux
    ids: [linux]
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*
      - "{{ .Binary }}"

  - id: windows
    ids: [windows]
    format: zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE*
      - README*
      - "{{ .Binary }}"
